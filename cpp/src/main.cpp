#include <stdio.h>
#include <stdint.h>
#include <cstdlib>

int main() {
    // Given a source/sources
    // Analyze photo/image and forking links
    printf("Running..");
    std::exit(EXIT_SUCCESS);
}